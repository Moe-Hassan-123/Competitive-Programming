#include <bits/stdc++.h>
using namespace std;
#define FASTIO ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

int main()
{
    //  freopen("input.in","r",stdin);
    FASTIO;
    int t;
    scanf("%d", &t);
    while (t--)
    {
        solve();
    }
    return 0;
}

void solve()
{
    
}